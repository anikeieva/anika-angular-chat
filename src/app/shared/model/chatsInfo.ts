export class ChatsInfo {
  name: string;
  avatar: string;
  message: string;

  constructor(
    name: string,
    avatar: string,
    message: string) {
    this.name = name;
    this.avatar = avatar;
    this.message = message;
  }
}
